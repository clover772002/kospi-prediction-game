/**
 * true: 집계 아이템은 설명·안내만 노출(블러 미리보기), API 차트·칩 잠금 해제 숨김.
 * 참여·표본이 충분해지면 false로 바꿉니다.
 */
export const INSIGHT_PRODUCTS_PREVIEW_ONLY = true;

/**
 * true: 상점 소모품은 목록 형태만 흐릿하게 보이고 구매 버튼·API 호출 없음.
 * 다시 오픈할 때 false로 바꿉니다.
 */
export const SHOP_CONSUMABLES_PREVIEW_ONLY = true;
